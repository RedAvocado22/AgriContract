meta {
  name: 12. Sign Contract (buyer — partial)
  type: http
  seq: 12
}

put {
  url: {{base_url}}/api/v1/contracts/{{contract_id}}/sign
  body: none
  auth: bearer
}

auth:bearer {
  token: {{buyer_token}}
}

tests {
  test("should return 200", function() {
    expect(res.getStatus()).to.equal(200);
  });
}
