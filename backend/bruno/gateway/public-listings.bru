meta {
  name: GET /api/v1/listings (public, no token)
  type: http
  seq: 2
}

get {
  url: {{base_url}}/api/v1/listings
  body: none
  auth: none
}

tests {
  test("should return 200 without token", function() {
    expect(res.getStatus()).to.equal(200);
  });
}
