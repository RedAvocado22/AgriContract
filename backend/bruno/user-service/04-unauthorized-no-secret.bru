meta {
  name: GET /api/v1/users/me (no token → 401)
  type: http
  seq: 5
}

get {
  url: {{base_url}}/api/v1/users/me
  body: none
  auth: none
}

tests {
  test("should return 401 without JWT", function() {
    expect(res.getStatus()).to.equal(401);
  });
}
