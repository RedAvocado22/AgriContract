meta {
  name: 10. [Error] Confirm delivery from OFFERED — invalid status (expect 400)
  type: http
  seq: 10
}

put {
  url: {{base_url}}/api/v1/contracts/{{contract_id}}/confirm-delivery
  body: none
  auth: bearer
}

auth:bearer {
  token: {{buyer_token}}
}

tests {
  test("confirm delivery from OFFERED should return 400", function() {
    expect(res.getStatus()).to.equal(400);
  });
}
